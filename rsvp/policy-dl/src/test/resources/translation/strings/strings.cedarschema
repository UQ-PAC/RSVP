entity Role enum [ "Important\tUser", "Powerful\nAdmin" ];
entity User enum [ "Carl", "Tim" ];

entity Account = {
    role: Role,
    alias: String
};

action auth appliesTo {
    principal: [ Account ],
    resource: Account
};

action "auth\tauth" appliesTo {
    principal: [ Account ],
    resource: Account
};

