entity Role enum [ "User", "Admin" ];

entity Account = {
    name: String,
    age: Long,
    role: Role,
    friends: Set<Account>
};

action introspect appliesTo {
    principal: Account,
    resource: Account
};


