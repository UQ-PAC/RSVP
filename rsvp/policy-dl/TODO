+ Equality/Inequality comparison between records (see common-type translation test)
+ AST support for entities alongside with source locations
+ Source locations for invariants and parser expressions generated by the invariants
+ Required field in types to be moved to attributes
+ Remove annotations from common types, as they are largely in the way (ignore for now)
