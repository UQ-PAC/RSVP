entity Role enum [ "User", "Admin" ];

entity Account = {
    friends: Set<Account>,
    permissions: {
        roles: Set<Role>
    }
};

action introspect appliesTo {
    principal: Account,
    resource: Account
};


