entity Role enum [ "Important\tUser", "Powerful Admin" ];
entity User enum [ "Carl", "Tim" ];

entity Account = {
    role: Role,
    alias: String
};

action auth appliesTo {
    principal: [ Account ],
    resource: Account
};

